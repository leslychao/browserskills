# Verification record — 2026-09-09

This record distinguishes the owned fixtures from live Yandex and actual model acceptance. It is not a deployment-ready claim.

## Confirmed locally

- Web unit tests after the LAN HTTP change: 19 passed; 92.53% line coverage, 86.84% branch coverage. Tests include stale instruction/nonce handling, media failure, session errors, manual selection and UUID generation without secure-context-only APIs.
- API Maven verify after request-local option mapping: 23 unit and 11 real-PostgreSQL integration tests passed without skips; 90.45% line coverage with no exclusions. This includes alias mapping, unknown/original-ID rejection and concurrent map isolation. Additional real chunked HTTP login above 32 KiB returned 413, followed by a successful normal login.
- Frontend Chromium tests with stub HTTP API: 2 passed, including audio UI, uncertain submission and mobile login layout.
- Full system fixture: 5 passed in 30.3 seconds after the final STOP/late-result synchronization and model option-mapping changes, with the production web bundle, real PostgreSQL17.9, Spring API, five independent Chromium profiles and actual FFmpeg/ffprobe. The model endpoint was a deterministic stub that selects the received model ID by option label. The scenarios verified 50 distinct sends with duplicate confirmations, UNKNOWN after a lost site response, owner isolation, original image bytes, original task audio and instruction-example audio, SHA256/Range, HTML audio playback, full instruction transmission and `input_audio` requests.
- The browser worker's final built-image Docker suite passed all 94 tests in19.08 seconds, with97.38% line coverage and84.33% branch coverage. This final run used the source/tests packaged in the image, with no source mounts. Regressions cover command admission during shutdown, a partially transmitted OPEN, idempotent server close and waiting for a cancelled pending Chromium launch to release its profile. Exact commands are in [apps/browser/README.md](../apps/browser/README.md) and [operations](operations.md).
- Full production web bundle → noVNC → Spring proxy → headed Docker Chromium test passed in8.0 seconds with the final API artifact and browser image. The worker uses a read-only root filesystem and the production supervisor. It verified the real1366×768 framebuffer, reconnection after page refresh, rejection of a competing authenticated session, revocation of the existing socket before automation, and socket closure on logout. The test checks reconnection through a completed replacement RFB handshake: Playwright clears its old WebSocket tracking on top-level navigation, so the old object's `isClosed()` cannot verify a reload.
- `npm run test:profile` passed against the final browser image with the current fixture/supervisor mounted read-only. A persistent HttpOnly cookie and localStorage value survived immediate Docker stop and replacement by a new container/hostname. Both stops exited0 and left no SingletonLock/Cookie/Socket. Report: `.cache/profile-lifecycle/browserskills-profile-f0498389-7b48-412b-a331-f1fb9afd0e8a/report.json`.
- API Docker image built successfully. `npm run test:docker-api` passed after the HTTP change with the actual image, PostgreSQL application role, non-root UID 10001, read-only root filesystem, embedded production web assets, private component readiness, HttpOnly/SameSite cookies without Secure, CSRF/login/logout and installed FFmpeg/ffprobe. No certificate files were supplied. Workers and inference were intentionally unavailable in this isolated smoke.
- Official Qwen2.5-Omni-7B weights were verified, converted to Q4_K_M + Q8_0 mmproj and loaded by the pinned llama.cpp CUDA server in a non-root/read-only container. The actual image/audio requests consume modality tokens. Model accuracy is measured separately; successful startup is not acceptance.
- Operations tooling originally passed14 unit tests plus Windows ACL checks, Compose validation and a real PostgreSQL dump/restore roundtrip. The HTTP update removed certificate generation and its retired checks; the current full roundtrip and remote-preparation checks are recorded below and in [operations](operations.md).
- The full deployment backup/restore fixture passed again after the HTTP change in159.84 seconds under an ordinary Windows operator token (`WindowsAdministrator=false`). Actual Flyway migrations and CLI provisioning created five users/assignments; completed runs/items/usage were SQL fixtures. Every database table digest and all five persistent cookie/localStorage readbacks matched after the real backup/restore scripts restored fresh volumes. Protected backup ACLs were verified. Stale dangling locks, an actual SIGKILL, a corrupted archive, a missing clean-shutdown flag, nonempty DB/profile volumes were rejected, with existing data preserved. Source and destination used identical immutable image IDs and frozen fixture/supervisor files. All three disposable projects and their volumes were removed. Report: `runtime/browserskills-roundtrip-96f2f913d617/report.json`, SHA256 `17d4413da9fe8962b5ac6fee49f97dbf9759d601ad2b69dda00637890a00940d`. These are owned HTTP fixture sessions, not proof that Yandex will retain login after transfer to another host.

Local workstation measurements belong to `192.168.0.109`, which is different from the requested deployment server. They cannot establish capacity or readiness of `192.168.0.107`.

The user-provided screenshot shows Windows, an RTX3070 and approximately32GiB RAM on the target machine. Initial SSH/RDP/WinRM/HTTPS and DCOM/SMB probes did not connect. The user subsequently supplied the existing Docker API `tcp://192.168.0.107:2375`; this endpoint works and is now the deployment transport. Its authentication/exposure settings were not changed.

Target preflight at2026-09-09T04:32:56Z confirmed Docker Desktop/WSL2, engine27.3.1,16 guest CPUs and16665694208bytes (15.52GiB) guest memory. The official pinned CUDA probe identified an RTX3070, driver610.62,8192MiB VRAM with6156MiB free. Guest MemAvailable was approximately11.67GiB and Linux backing-filesystem free space approximately916.85GiB. These are Docker guest measurements, not Windows physical RAM/drive capacity. No BrowserSkills resources existed before deployment; existing containers from another project were preserved. Report: `runtime/remote-107-preflight.json`.

At2026-09-09T04:52:47Z, the actual API/web, PostgreSQL and five worker containers were deployed on `.107`. External HTTPS liveness passed with an explicit private CA and normal chain/serverAuth/IP validation, without changing the Windows trust store. The ordinary `vitalii` operator was provisioned through the account CLI; its generated password remains in the protected deployment directory. Readiness reported database and all five workers UP, `manualReviewAvailable=true`, and inference DOWN. Actual container inspection confirmed separate worker networks/profile/token volumes, read-only worker roots, dropped capabilities, nonprivileged containers and the Chromium seccomp JSON; only `192.168.0.107:8443` was published. No remote host bind mounts were used. Evidence: `runtime/remote-107/71a2479ad7764a008143936e204f355c/deployment-result.json`.

The initial Codex in-app browser navigation to `https://192.168.0.107:8443` returned `ERR_CERT_AUTHORITY_INVALID`. The user declined certificate installation and explicitly selected ordinary LAN HTTP. No Windows trust-store change or browser-warning bypass occurred. The migration to `http://192.168.0.107:8080` completed with API image `sha256:235c84d251fc91a5ff3fac02d2643bb119e4c8dbf41df3607b7c48fb5285fb3e`; the other seven container IDs, verified model hash, credentials and volumes were preserved. Only8080 is now published. The HTTP auth smoke passed static assets, private readiness, anonymous rejection, CSRF, session rotation, HttpOnly/SameSite without Secure, login/status/history/logout. Evidence: `lan-http-result.json` and `auth-http-smoke-result.json` in the protected deployment directory.

The actual Codex in-app browser successfully opened the HTTP app. The rendered login page, disabled empty-form submission and rejected incorrect password were checked. The synthetic password was removed and the clean page left ready for user login. Interactive noVNC/Chromium checks on this installation remain pending login in that browser; API auth success alone does not prove them.

The web UI's two secure-context-only `crypto.randomUUID()` calls were replaced by one UUIDv4 generator using `crypto.getRandomValues()`, which is available on LAN HTTP. Regression tests cover starting a run and confirming its exact task while `randomUUID` is unavailable. All19 web tests passed (92.53% line coverage), and production typecheck/build passed. This does not substitute for the requested deployed-browser check.

The packaged API now also defaults to HTTP8080 without certificate-file settings. Its local image `sha256:c8a6fc1c13d11aa4aa07b8b0f82fcc1240c74a579ba05c9b1669016afddc5412` passed the actual production Docker API/PG smoke without any TLS files: process isolation, static assets, private readiness, HTTP cookie/CSRF/login/logout and FFmpeg. This default-cleanup image has not replaced the live `235c84...` image, whose explicit environment already selects the identical HTTP behavior; the user-facing installation is being kept stable during interactive testing.

The three serving-model files were transferred to `.107` in266.01s with identical source/target SHA256 and a new-only publication manifest. Actual inference startup reached READY in20.63s without changing the agreed model/context settings; the RTX3070 reported6696MiB GPU usage and1323MiB free, with no OOM/restarts. DB and all five workers also reported UP. Evidence: `runtime/remote-107-model-transfer-20260909T045056Z-f13b25f7.json` and `runtime/remote-107-inference-start-20260909T045939Z-a7247a4e.json`. The frozen100-case target evaluation completed separately and failed its quality gates, as recorded below.

## Live-site observation

The user corrected the actual target to `https://yang.yandex-team.ru/?activeTab=all` and confirmed corporate sign-in. The real Yang landing page showed its service title, documentation link and Login redirect to `passport.yandex-team.ru`. After explicit authorization to use the supplied credentials, sign-in advanced to a six-digit one-time-code challenge; credential values were not persisted to project files. Authenticated Yang tasks and instructions have not yet been inspected.

Source OPEN now uses the fixed Yang catalogue URL and the production adapter checks the Yang origin. UI branding and corresponding expectations were updated to Yandex Yang. No guessed template selectors or old Tasks origin fallback were added. These source changes are not in the currently deployed `.107` API/browser images. The default-navigation regression also verifies that Yang without a reviewed descriptor fails as UNSUPPORTED_TEMPLATE and the old Tasks origin fails as UNSUPPORTED_ORIGIN.

After the target correction, all workspace/E2E TypeScript checks,19 web tests (92.53% line coverage), both owner unit tests and the Vite production web build passed. The browser esbuild command could not complete because the restricted Windows token cannot read an ancestor directory; no compiler fallback or permission change was added. New Docker builds/deployment remain unverified.

Two additional operations checks were prepared before the target correction: ordinary-HTTP RFB testing at the fixture-only `rfb-http.test` origin, and binary-stream remote backup/restore of PostgreSQL and five profiles through an explicit Docker API. The RFB test asserts `window.isSecureContext === false`, painted framebuffer pixels, reconnect and ownership/revocation. Its changed code typechecks but the actual new run is pending. The remote scripts pass PowerShell parsing; the delegated roundtrip's final outcome and cleanup could not be retrieved after automatic approval review failed due to the Codex usage limit. Neither prepared check is counted as passed. Previous successful local HTTP backup and localhost RFB evidence remain separate above.

The following Tasks observations are historical work against the earlier, incorrect target. They provide no evidence for Yang selectors, task identities, iframe/media origins or instruction completeness.

The Chrome session exposed the Yandex Tasks demo catalogue. The image-classification project displayed its complete instruction in a modal iframe, with a separate `/instructions/{poolId}` link. Opening the unpaid training navigated to `/task/{poolId}/{taskSuiteId}` and embedded `https://iframe-tasks.yandex`.

The embedded body contained only whitespace, including after one reload. The top-level Send/Skip/Exit buttons existed, but no question, image or answer controls were available for inspection. No answers were selected or submitted. The route's suite identifier must not be assumed to identify one individual task. A loaded form is still needed to verify task boundaries, controls and success acknowledgement.

A second independent unpaid demo, search-query classification (pool `5221474`), exposed its complete four-category instruction with examples, but its task iframe again contained only whitespace. Console warnings reported access to index 0 of an empty MobX array; these warnings do not establish the root cause. No form selectors or submission identity could be verified in either demo. The second page was left open for user continuation.

The user later opened pool6685530 (registration-on-a-platform task). Its Chrome screenshot still showed a central loading indicator and no task controls; the user requested the next check through BrowserSkills' own server browser. Production adapter profiles remain empty until a loaded compatible form is available. Audio-task templates have not been observed on the live site.

## Actual model diagnostics

The final fixed 100-case synthetic corpus completed on the target `.107` RTX3070 with the production system prompt, request-local option aliases, disabled prompt/idle caches, max_tokens512 and the API's bounded FFmpeg mono16kPCM16 normalization. No retries or selection of favourable aliases were used. The report stores the actual aliases, corpus/evaluator/prompt hashes and resource samples for replay.

| Category | Correct | Accuracy | p95 latency | Acceptance |
|---|---:|---:|---:|---|
| Text | 22/25 | 88% | 0.449s | Fail |
| Image | 20/25 | 80% | 3.278s | Fail |
| Speech | 25/25 | 100% | 12.673s | Pass for this diagnostic set |
| Sound/prosody | 7/25 | 28% | 35.672s | Fail |

The evaluator exited1 because text, image and sound/prosody accuracy did not meet90%. All100 unique cases completed:74 CORRECT,26 UNSOLVED, no inference errors or exhausted evaluation budget. All category latency gates passed. Peak inference-container memory was7602909184bytes (7.081GiB); whole-device GPU memory reached6702MiB across775 samples. The container stayed healthy with no OOM or restarts. Speech used an English Windows SAPI voice; the set does not establish Russian-speech, natural-sound, music or live-Yandex quality.

Target report: `runtime/model-evaluation-remote-107-20260909T050829Z.json`, SHA256 `415693e36d42436efa693f260bff0f7e50c7c5c9fcd00cbc856823b433fa9322`. The adjacent `.target.json` records the explicit Docker endpoint, engine/container/image IDs and model hashes. Corpus, evaluator, prompt hashes and request settings match the final `.109` run; the randomly generated option aliases differ, so accuracy differences cannot be attributed solely to the GPU. The wrapper now accepts explicit `DockerHost` and ordered `ComposeFile` arguments without changing the caller's Docker context or overwriting reports.

Controlled probes also showed that a correctly extracted custom classification rule can still cause the model to abstain. A short internal finding before the decision did not resolve the instruction/sound failures and was not added to production. Full instruction capture and preservation are verified; reliable model interpretation of every project rule is still an open acceptance condition.

Historical `.109` RTX5080 result: text96%/p95 0.223s, image76%/2.999s, speech100%/12.376s, sound/prosody24%/35.341s; its quality gate also failed. Peak container memory was2814382080bytes and whole-device GPU memory6879MiB across726 samples. Report: `runtime/model-evaluation-final-109.json`, SHA256 `00ea5f362412f3e34b53d0eb05c189ec8b172203f6082fd8c799b6c978f42784`. The earlier baseline is retained separately in `runtime/model-evaluation-baseline-109.json`; several request settings changed between these runs, so their difference is not attributed to one change alone. Model provenance is in `runtime/model-provenance.json`. The temporary local GPU smoke container was stopped after measurement; its verified model volume remains available.

The original HTTPS API image was `sha256:e8166373404b260f7ae4c50d36b004cfcf7b813ff640b430dabd57306fee59f6`; its replacement HTTP deployment and final default-cleanup build are recorded above. The unchanged browser image `sha256:b8d458c3ef6a94d6cfd7e33b1cb945667bd02f0baf8e3789a1c5de5a37676234` and inference image `sha256:d5aaca724e6214bd4cb45752dbe57fa2e6a9ce7ee63b595425cac66cf94bb2b4` are deployed on `.107`. Release acceptance remains pending.

The deployment-fixture checks exposed defects that the earlier process-level tests did not cover: unquoted inline Compose tmpfs entries became invalid mount targets; Chromium needed writable XDG directories on a read-only root; simultaneous display shutdown and duplicate Playwright/application signal handlers prevented clean profile flush. These were reproduced before their fixes. The production supervisor now keeps X alive until Chromium closes, the application owns TERM/INT, shutdown refuses new commands and awaits pending launch cleanup, and backup refuses an unclean/still-locked profile. No profile-lock deletion or permission relaxation was used to make the tests pass.

## Remaining acceptance

- A supported, loaded Yang task template, with its complete instruction and verified one-task submission boundary, followed by deployment of the corrected target.
- Independently labelled accuracy/performance evaluation meeting all category gates. Generated diagnostic shapes/tones are not a substitute for representative speech, environmental sounds, music or prosody.
- Complete interactive noVNC/task testing on `192.168.0.107` and restart after Windows sign-in. API/web/PG/five-worker/inference deployment, HTTP auth, the rendered login page and target-model performance are verified; model quality gates failed. Local five-profile backup/restore passed again after the HTTP change; target-host recovery and real Yandex session validity remain unverified.
